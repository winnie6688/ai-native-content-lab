# Shared

Shared rules and reusable context across platform Skill groups.

Planned files:

- `color-system.md`: common brand color tokens
- `writing-principles.md`: writing and trust principles
- `fact-checking-rules.md`: general factual verification rules

Platform-specific rules should still live inside each platform folder, such as `wechat/`.
